# Артефакты сборки и служебные файлы (на уровне всего репозитория)
**/.dart_tool/
**/.idea/
**/build/
**/.flutter-plugins-dependencies
**/*.iml

# Прочее
.DS_Store
openhands/.env
telegram-agent/.env

# Сгенерированный артефакт для доставки бэкенда (не хранится в git)
chatbiz-backend.zip
