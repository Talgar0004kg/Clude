# 📋 План проекта: AI-платформа чат-ботов для бизнеса

> Статус: **план согласовывается**. Код продукта ещё не пишем.
> Ветка разработки: `claude/website-visualization-project-ox95zr`
> Дата создания плана: 2026-06-14

---

## 1. 💡 Идея

SaaS-платформа (один сайт — много бизнесов), где каждый бизнес получает
**AI-бота** для общения с клиентами **прямо на сайте по уникальной ссылке**.

### Почему чат на сайте, а не в мессенджере
- **Telegram** — в Бишкеке/Кыргызстане мало кто использует → отпадает.
- **WhatsApp как основной чат** — WhatsApp Business API банит за массовые/быстрые
  ответы → рискованно делать основным каналом.
- **Решение:** чат живёт у нас на сайте. WhatsApp используем только для
  **уведомлений бизнесу о заявке** (мало сообщений → риск низкий).

### Как работает
1. У каждого бизнеса (барбершоп, салон красоты, фастфуд, риелтор и т.д.) —
   своя **страница-чат по уникальной ссылке**.
2. Клиент открывает ссылку → попадает в чат с AI-ботом этого бизнеса.
3. Бот работает на **DeepSeek API**. Все боты — на одном API, но у каждого
   **свои данные**.
4. В **личном кабинете** бизнес сам заполняет инфо (услуги, цены, время, окошки)
   → из этого собирается **MD-файл** = «мозг»/системный промпт бота.
   Бот ведёт себя как сотрудник.
5. Бот консультирует по данным бизнеса (например: «свободно в 15:00»).
6. Когда бот по контексту понимает, что клиент **хочет записаться/купить** →
   берёт **мини-анкету**: как обращаться + номер телефона.
7. Заявка автоматически уходит:
   - **(а)** в личный кабинет бизнеса;
   - **(б)** на **WhatsApp** бизнеса (из настроек) через whatsapp-web.js.
   - СМС — позже (дорого).
8. **Статистика:** посещения по ссылке, начатые переписки, доведённые до заказа.
9. Бизнес видит **все истории переписок** бота с клиентами.
10. **Модерация:** если бизнес сам настроил бота → заявка админу → после
    одобрения бот включается и получает ссылку. Админ тоже может настраивать.

---

## 2. 👥 Роли в системе

| Роль | Кто это | Что делает |
|------|---------|------------|
| 🛡️ **Супер-админ** | владелец платформы | одобряет ботов, видит все бизнесы, модерация |
| 🏢 **Бизнес / компания** | клиент платформы | настраивает бота, видит заявки (CRM), историю, статистику |
| 💬 **Клиент** | посетитель по ссылке | общается с ботом, оставляет заявку |

---

## 3. 🏗️ Архитектура (согласовано)

| Часть | Где живёт | Что делает |
|-------|-----------|------------|
| 🎨 **Фронтенд (React)** | **GitHub Pages** | лендинг, чат, кабинет бизнеса, админка |
| ⚙️ **Бэкенд (Node.js, 24/7)** | **отдаётся ZIP-файлом** | DeepSeek, авторизация, whatsapp-web.js, статистика |
| 🗄️ **База данных** | **в том же ZIP** | бизнесы, боты, переписки, заявки, статистика |

**Почему так:** whatsapp-web.js запускает браузер (Chromium) и держит живую
сессию WhatsApp → ему нужен постоянно работающий сервер. GitHub Pages и Vercel
для этого не годятся. Поэтому бэкенд + БД отдаются как отдельный пакет (ZIP),
который запускается на своём сервере / локально.

---

## 4. 🧰 Технологии (предложение)

- **Фронтенд:** React + Vite, роутинг, i18n (рус/кыргыз), деплой на GitHub Pages.
- **Бэкенд:** Node.js + Express (REST API).
- **База данных:** SQLite (файл, без отдельного сервера — проще запускать из ZIP);
  при росте легко перейти на PostgreSQL.
- **AI:** DeepSeek API (ключ хранится только на бэкенде).
- **WhatsApp:** whatsapp-web.js (вход по QR, отдельный номер для рассылки).
- **Авторизация:** JWT (токены), пароли — хешируются.

---

## 5. 🧩 Модули и экраны

### 5.1 Фронтенд
- 🏠 **Лендинг платформы** — что это, для кого, как подключиться.
- 💬 **Страница-чат клиента** (по ссылке) — брендирование бизнеса (лого, цвет,
  название), окно чата с ботом, форма-анкета при заказе.
- 🔐 **Вход / регистрация бизнеса.**
- 🏢 **Кабинет бизнеса:**
  - настройка бота (форма → собирается MD-«мозг»);
  - заявки (мини-CRM со статусами: *новая → связались → записан → выполнено*);
  - история переписок бота с клиентами;
  - статистика (посещения, переписки, заказы, конверсия);
  - настройки WhatsApp-номера и брендирования.
- 🛡️ **Админ-панель:** список бизнесов, одобрение/отклонение ботов, общая
  статистика.

### 5.2 Бэкенд (API)
- авторизация (бизнес, админ);
- CRUD бизнесов и ботов;
- генерация MD-«мозга» бота из формы;
- чат-эндпоинт → DeepSeek (с учётом MD-данных бизнеса);
- определение намерения «хочет заказать» → запуск сбора анкеты;
- сохранение заявок + переписок в БД;
- отправка заявки в WhatsApp (whatsapp-web.js);
- статистика (счётчики событий: визит, начало чата, заказ);
- модерация (одобрение ботов админом);
- защита от спама / лимиты на DeepSeek (контроль расходов).

---

## 6. 🤖 Логика бота

1. **MD-«мозг»** собирается из формы бизнеса (услуги, цены, расписание, правила).
   Передаётся боту как системный промпт.
2. Бот **консультирует** по этим данным на языке клиента (рус/кыргыз).
3. Бот **отслеживает намерение** заказа/записи. Как только клиент хочет
   заказать → переключается в режим сбора анкеты (имя + телефон).
4. После анкеты → заявка уходит в кабинет + WhatsApp, клиенту — подтверждение.

**Расписание/«свободные окошки»:** на старте — статичный текст (бизнес сам
вписывает). Живой календарь — на будущее.

---

## 7. 📲 WhatsApp (whatsapp-web.js)

- Вход по **QR-коду** (сканируется один раз WhatsApp-ом компании).
- Отправляет бизнесу уведомление о новой заявке (имя + телефон + контекст).
- ⚠️ Неофициальный способ → риск бана при большом объёме. **Совет:**
  использовать **отдельный номер** под рассылку, не личный. Объём низкий
  (только уведомления) → риск низкий.
- СМС — отложено (дорого).

---

## 8. 📊 Статистика

Счётчики на каждый бизнес/ссылку:
- посещения страницы по ссылке;
- начатые переписки;
- доведённые до заказа (заявки);
- конверсия (заказы / визиты);
- количество переписок и доступ к их истории.

---

## 9. 🌐 Языки

Интерфейс и бот — **двуязычные: русский + кыргызский**, с переключателем.
Английский — возможно позже.

---

## 10. ✅ Согласованные решения

| Вопрос | Решение |
|--------|---------|
| Тип проекта | Делаем по-настоящему (реальный DeepSeek + бэкенд) |
| Фронтенд | React, хостинг — GitHub Pages |
| Бэкенд + БД | Отдаётся ZIP-файлом в чат (запуск на своём сервере) |
| Демо | Несколько примеров бизнесов |
| Язык интерфейса | Оба (рус + кыргыз) |
| DeepSeek ключ | Есть у владельца |
| WhatsApp | whatsapp-web.js |

---

## 11. 🗺️ Порядок работы (когда начнём писать код)

1. Каркас React + дизайн-система + двуязычность (рус/кыргыз).
2. Лендинг платформы.
3. Страница-чат клиента (с брендированием) + подключение DeepSeek.
4. Логика бота (MD-«мозг» + сбор анкеты).
5. Кабинет бизнеса (настройка бота, заявки/CRM, история, статистика).
6. Админ-панель (бизнесы, одобрение).
7. WhatsApp-уведомления (whatsapp-web.js).
8. Бэкенд + БД → упаковка в ZIP.
9. Деплой фронтенда на GitHub Pages.

---

## 12. ❓ Открытые вопросы / на будущее

- Монетизация (подписка за бота в месяц?).
- Живой календарь записи (вместо статичного расписания).
- Виджет-кнопка чата для вставки на свой сайт бизнеса.
- Передача диалога живому оператору (human handoff).
- Переход с SQLite на PostgreSQL при росте нагрузки.
- Лимиты DeepSeek по тарифам (контроль расходов).
